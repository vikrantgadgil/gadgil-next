import { ReadyReckoner } from "@/components/urdu/ReadyReckoner";

export const metadata = {
  title: "Ready Reckoner — Urdu Alphabet",
  description:
    "Browse all Urdu letters with their positional forms — isolated, initial, medial, and final.",
};

export default function ReadyReckonerPage() {
  return <ReadyReckoner />;
}
